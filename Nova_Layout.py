#!/usr/bin/python3

#to run this script in Pcbnew enter:
#   import os (1 time only)
#   with open(os.path.expanduser('<path_to_script>')) as <a_name_for_the_file>: exec(<a_name_for_the_file>.read())
# a ... should appear, then press Enter

#import pcbnew
#set a board object
#board = pcbnew.GetBoard()

print("The script executed.")
